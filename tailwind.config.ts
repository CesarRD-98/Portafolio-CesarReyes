import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
  content: [],
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-fira-sans)'],
      },
    },
  },
  plugins: [],
};

export default config;
